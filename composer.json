{
    "name": "teelake/billo",
    "description": "NRS-focused invoicing SaaS",
    "require": {
        "php": ">=8.1",
        "dompdf/dompdf": "^3.0",
        "ext-curl": "*"
    },
    "suggest": {
        "stripe/stripe-php": "Required when payments.provider is \"stripe\" in config."
    },
    "config": {
        "sort-packages": true
    }
}
